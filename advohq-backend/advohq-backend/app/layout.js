// This file is intentionally minimal — the project is an API-only backend.
// No React UI is served from this deployment.
export const metadata = { title: 'AdvoHQ API' };
export default function RootLayout({ children }) { return children; }
