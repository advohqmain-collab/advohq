/**
 * AdvoHQ — frontend config
 * ────────────────────────
 * Points at the live backend (Next.js API deployed on Render),
 * INCLUDING the trailing "/api".
 */
window.ADVOHQ_API_URL = 'https://advohq.onrender.com/api';
