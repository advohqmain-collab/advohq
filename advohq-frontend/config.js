/**
 * AdvoHQ — frontend config
 * ────────────────────────
 * Set this to your deployed backend URL (the Next.js API on Render),
 * INCLUDING the trailing "/api".
 *
 *   ┌──────────────────────────────────────────────────────────────┐
 *   │  EDIT THE LINE BELOW: replace YOUR-RENDER-APP with the real   │
 *   │  service name from your Render dashboard.                     │
 *   └──────────────────────────────────────────────────────────────┘
 *
 * Example: if your Render service is "advohq-backend", the URL is
 *   https://advohq-backend.onrender.com/api
 */
window.ADVOHQ_API_URL = 'https://YOUR-RENDER-APP.onrender.com/api';
